import Box from '@mui/material/Box'
function BoardFooter() {
    return ( <Box sx={{

        width: '100%',
        height: (theme) => theme.varCus.footerH,
        display: 'flex',
        alignItems: 'center'
    }}>
footer
    </Box> )
}

export default BoardFooter